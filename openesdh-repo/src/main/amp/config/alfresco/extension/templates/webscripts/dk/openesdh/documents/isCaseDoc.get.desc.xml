<webscript>
    <shortname>Is case document</shortname>
    <description>
        Returns true if the document is part of a case and script is accessed via a nodeRef.
        When accessed with a caseId returns some additional case details if true.(Used for the module Evaluator)
        Otherwise returns false.
    </description>
    <url>/api/openesdh/documents/isCaseDoc/{caseId}</url>
    <url>/api/openesdh/documents/isCaseDoc/{store_type}/{store_id}/{id}</url>
    <format default="json"></format>
    <authentication>user</authentication>
    <transaction>required</transaction>
    <family>Case Document Tools</family>
</webscript>