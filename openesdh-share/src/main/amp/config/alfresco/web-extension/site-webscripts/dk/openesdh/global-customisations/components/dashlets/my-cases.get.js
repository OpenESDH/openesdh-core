model.jsonModel = {
    rootNodeId: args.htmlid,
    pubSubScope: instance.object.id,

    widgets: [
        {
            id: "DASHLET",
            name: "alfresco/dashlets/MyCases"
        }
    ]
};